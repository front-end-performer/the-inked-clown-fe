import { Abril_Fatface, Inter } from "next/font/google";

export const abril_fatface_init = Abril_Fatface({
  subsets: ["latin"],
  weight: "400",
  variable: "--font-abril",
});
export const inter_init = Inter({ subsets: ["latin"] });
